image: node:10

pipelines:
  branches:
    # master:
    #   - step:
    #       script:
    #         - docker login --username $DOCKER_USERNAME --password $DOCKER_PASSWORD
    #         - yarn docker:release

options:
  docker: true